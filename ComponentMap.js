import React, {
  Text,
  ListView,
  TextInput
} from 'react-native';

import MapExample from './MapExample';
import Button from './Button';
export default components = {
	LABEL: Text,
	BUTTON: Button,
	MAP: MapExample,
	LIST: ListView,
	TEXTFIELD: TextInput,
	PIN: null
};
